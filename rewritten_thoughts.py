thoughts = ["I can't code", "I can't focus", "I can't finish this"]

rewritten_thoughts = list(map(lambda x: x.replace("I can't", "I'm learning to"), thoughts))
print(rewritten_thoughts)
                          
